# RSS 阅读器环境变量模板
# 复制此文件为 .env 并填入你的实际配置值

# ============================================
# 数据库配置
# ============================================

# 开发环境 - PostgreSQL (推荐)
DATABASE_URL="postgresql://username:password@localhost:5432/rss_reader?schema=public"
DIRECT_URL="postgresql://username:password@localhost:5432/rss_reader?schema=public"

# Vercel Postgres (生产环境)
# Vercel 会自动注入这些变量，你只需要在 Vercel 设置中配置：
# DATABASE_URL="${POSTGRES_URL}"
# DIRECT_URL="${POSTGRES_URL_NON_POOLING}"

# 本地开发也可以使用 SQLite（需要修改 prisma/schema.prisma）
# DATABASE_URL="file:./dev.db"


# ============================================
# NextAuth.js 配置
# ============================================

# 应用 URL
# 开发环境
NEXTAUTH_URL="http://localhost:3000"

# 生产环境（部署时修改为你的域名）
# NEXTAUTH_URL="https://your-production-domain.com"

# NextAuth 密钥（必需，至少 32 字符）
# 生成命令: openssl rand -base64 32
NEXTAUTH_SECRET="your-generated-secret-key-here-min-32-chars"


# ============================================
# Google OAuth 2.0 配置
# ============================================

# 从 Google Cloud Console 获取这些凭据
# 配置步骤：https://console.cloud.google.com/

# Google 客户端 ID
GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"

# Google 客户端密钥
GOOGLE_CLIENT_SECRET="your-client-secret"


# ============================================
# 授权重定向 URI 配置指南
# ============================================
#
# 在 Google Cloud Console 中配置以下重定向 URI：
#
# 开发环境：
# http://localhost:3000/api/auth/callback/google
#
# 生产环境：
# https://your-domain.com/api/auth/callback/google
#
# ============================================


# ============================================
# 翻译服务配置
# ============================================
#
# Google Translate API 密钥（可选）
# 如果未设置，翻译功能将被禁用
# 获取方式：https://cloud.google.com/translate/docs/setup
#
GOOGLE_TRANSLATE_API_KEY="your-google-translate-api-key-here"
#
# 注意：Google Translate API 是付费服务，但有免费额度
# 免费额度：每月前 500,000 字符免费
#
# ============================================


# ============================================
# Vercel Cron 配置
# ============================================
#
# Cron Secret（用于保护定时任务端点）
# 生成命令: openssl rand -base64 32
# 这个密钥用于验证 Vercel Cron 请求的合法性
#
CRON_SECRET="your-cron-secret-key-here-min-32-chars"
#
# 注意：
# 1. 本地开发和生产环境使用不同的 CRON_SECRET
# 2. 生产环境在 Vercel 项目设置中配置此环境变量
# 3. Vercel Cron 会自动在请求头中携带此 secret
#
# ============================================
# 管理员配置
# ============================================
#
# 管理员邮箱（多个邮箱用逗号分隔）
# 这些邮箱的用户将自动获得管理员权限
#
ADMIN_EMAILS="admin@example.com,admin2@example.com"
#
# 注意：管理员可以访问 /admin 路径下的所有后台管理功能
#
# ============================================


# ============================================
# 安全提示
# ============================================
#
# 1. 永远不要将 .env 文件提交到版本控制系统
# 2. 使用强随机字符串作为 NEXTAUTH_SECRET
# 3. 生产环境必须使用 HTTPS
# 4. 定期轮换敏感凭据
# 5. 限制 Google OAuth 的测试用户（开发阶段）
# 6. 发布前将 OAuth 同意屏幕状态改为"已发布"
#
# ============================================

